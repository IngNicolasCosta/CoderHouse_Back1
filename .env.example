# Puerto en el que se levanta el servidor Express
PORT=

# Entorno de ejecucion: development | production | test
NODE_ENV=

# String de conexion a MongoDB Atlas
# Ejemplo: mongodb+srv://<usuario>:<password>@<cluster>.mongodb.net/<db>?retryWrites=true&w=majority
MONGO_URI=
